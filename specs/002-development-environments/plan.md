# Implementation plan

Add devenv inputs and a lock, explicit native library discovery, argv-based container adapters, platform-aware checks, and CI with path filters and cancellation. Retain full Linux archive and Docker runtime gates. Do not change the archive engine or weaken Linux publication requirements. Apple execution requires a supported Mac and Linux Nix builder.
